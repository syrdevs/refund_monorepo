{
  "name": "@vitacore/shared-ui",
  "version": "1.0.0",
  "private": true,
  "main": "build/dist/index.js",
  "dependencies": {
    "react-promise": "^2.0.3",
    "react-scripts-ts": "3.1.0"
  },
  "scripts": {
    "compile-ts": "tsc -p tsconfig.build.json",
    "compile-ts-watch": "tsc -p tsconfig.build.json -w --preserveWatchOutput",
    "start": "yarn clean && yarn run compile-ts-watch",
    "build": "yarn clean && yarn run compile-ts",
    "clean": " "
  },
  "devDependencies": {
    "prettier": "^1.15.3",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^3.2.1"
  }
}
